# 杜杜工作台 · 永久部署指南

## 方案一：Vercel 部署（推荐，最简单）

### 第1步：注册 GitHub 账号
1. 打开 https://github.com/signup
2. 输入邮箱、设置密码、用户名
3. 验证邮箱

### 第2步：上传文件到 GitHub
1. 登录 GitHub 后，点右上角 **`+`** → **`New repository`**
2. Repository name 填 `dudu-workbench`
3. 选 **`Public`**（公开）
4. 勾选 **`Add a README file`**
5. 点 **`Create repository`**
6. 点上方 **`Add file`** → **`Upload files`**
7. 把 `deploy/index.html` 拖进去
8. 点 **`Commit changes`**

### 第3步：注册 Vercel 账号
1. 打开 https://vercel.com/signup
2. 点 **`Continue with GitHub`**，用 GitHub 账号登录
3. 授权 Vercel 访问你的 GitHub

### 第4步：导入项目
1. 登录后点 **`Add New...`** → **`Project`**
2. 在列表中找到 `dudu-workbench`，点 **`Import`**
3. Framework Preset 选 **`Other`**
4. 点 **`Deploy`**

### 第5步：完成！
- 等待约10秒，显示 `Congratulations!`
- 你会获得一个永久链接，类似：
  ```
  https://dudu-workbench-xxxxx.vercel.app
  ```
- 这个链接**永久有效**，不会失效！

### 以后怎么更新内容？
- 在 GitHub 仓库里编辑 `index.html`，修改后保存
- Vercel 会自动重新部署，约30秒后更新生效

---

## 方案二：GitHub Pages 部署（备选）

### 第1-2步：同方案一（注册GitHub + 上传文件）

### 第3步：开启 Pages
1. 进入你的仓库 `dudu-workbench`
2. 点上方 **`Settings`** 标签
3. 左侧菜单找到 **`Pages`**
4. Source 选 **`Deploy from a branch`**
5. Branch 选 **`main`**，文件夹选 **`/ (root)`**
6. 点 **`Save`**

### 第4步：完成！
- 等待约1分钟
- 你会获得永久链接：
  ```
  https://你的用户名.github.io/dudu-workbench/
  ```

---

## 方案三：Netlify 部署（也简单）

### 第1步：打开 Netlify Drop
1. 浏览器打开 https://app.netlify.com/drop
2. 注册账号（可用 GitHub 登录）

### 第2步：拖拽上传
1. 把 `deploy` 文件夹直接拖到页面上
2. 自动部署完成！

### 第3步：完成！
- 获得永久链接，类似：
  ```
  https://dudu-workbench-xxxxx.netlify.app
  ```
- 还可以自定义域名

---

## 📱 部署成功后安装到 iPhone

1. 用 Safari 打开你的永久链接
2. 点底部 **`分享`** → **`添加到主屏幕`**
3. 点 **`添加`**
4. 主屏幕出现 🌿 杜杜工作台 App 图标
5. 以后点图标直接全屏打开，再也不用输网址了！

---

## ❓ 常见问题

**Q: 需要花钱吗？**
A: 完全免费。Vercel、GitHub Pages、Netlify 的免费额度对个人使用绰绰有余。

**Q: 链接会失效吗？**
A: 不会。只要你不删 GitHub 仓库，链接永久有效。

**Q: 怎么更新新闻内容？**
A: App 已经接入实时热搜 API，每次打开自动获取最新热点，不需要手动更新。如果要改任务内容，在 GitHub 上编辑 `index.html` 即可。

**Q: 可以改 App 的图标和名字吗？**
A: 可以。编辑 `index.html` 文件开头的 `<meta>` 标签，修改 `apple-mobile-web-app-title` 和图标链接。

**Q: 微信里能直接打开吗？**
A: 微信会限制外部链接。建议用 Safari 打开永久链接，然后添加到主屏幕。微信内打开时会提示复制链接用浏览器打开。
